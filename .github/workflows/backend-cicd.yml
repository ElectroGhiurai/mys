name: Backend CI/CD

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - 'backend/mys/**'
      - '.github/workflows/backend-cicd.yml'
  pull_request:
    branches: [ "main", "master" ]
    paths:
      - 'backend/mys/**'
      - '.github/workflows/backend-cicd.yml'
  workflow_dispatch:

jobs:
  pmd:
    name: PMD Code Quality Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend/mys
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'
        
    - name: Make mvnw executable
      run: chmod +x mvnw

    - name: Run PMD Code Quality Check
      run: ./mvnw pmd:check pmd:cpd-check

  spotbugs:
    name: SpotBugs Security Scan
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend/mys
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'
        
    - name: Make mvnw executable
      run: chmod +x mvnw

    - name: Run SpotBugs Security Scan
      run: ./mvnw compile spotbugs:check

  dependency-check:
    name: OWASP Dependency Scan
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend/mys
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'
        
    - name: Make mvnw executable
      run: chmod +x mvnw

    - name: Run OWASP Dependency Scan
      run: ./mvnw dependency-check:check

  build-and-test:
    name: Build & Test
    needs: [pmd, spotbugs, dependency-check]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend/mys
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'
        
    - name: Make mvnw executable
      run: chmod +x mvnw
        
    - name: Run Tests and Build (Skip static checks)
      run: ./mvnw clean verify -Dpmd.skip=true -Dspotbugs.skip=true -Ddependency-check.skip=true
      
    - name: Upload Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: backend-jar
        path: backend/mys/target/*.jar
        retention-days: 7

  deploy:
    name: Deliver to GitHub Releases
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    steps:
    - name: Download Build Artifact
      uses: actions/download-artifact@v4
      with:
        name: backend-jar
        path: ./backend-artifacts

    - name: Create Release and Upload Asset
      uses: softprops/action-gh-release@v2
      with:
        tag_name: release-${{ github.run_number }}
        name: Release ${{ github.run_number }}
        body: "Automated release of the backend application from GitHub Actions pipeline."
        files: ./backend-artifacts/*.jar
